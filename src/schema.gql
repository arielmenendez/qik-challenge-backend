# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AccountSummary {
  balance: Float!
  totalCredits: Float!
  totalDebits: Float!
}

type AccountType {
  balance: Float!
  createdAt: DateTime!
  id: ID!
}

type BalanceHistoryPoint {
  amount: Float!
  balance: Float!
  date: DateTime!
  transactionId: ID!
  type: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  createAccount: AccountType!
  credit(accountId: ID!, amount: Float!, description: String): TransactionTypeGQL!
  debit(accountId: ID!, amount: Float!, description: String): TransactionTypeGQL!
}

type Query {
  account(id: ID!): AccountType!
  accountBalance(id: ID!): Float!
  accountSummary(accountId: ID!): AccountSummary!
  accountsStatus: String!
  balanceHistory(accountId: ID!): [BalanceHistoryPoint!]!
  myAccounts: [AccountType!]!
  transactions(accountId: ID!, from: DateTime, limit: Int, offset: Int, to: DateTime, type: TransactionType): TransactionsPage!
}

enum TransactionType {
  CREDIT
  DEBIT
}

type TransactionTypeGQL {
  amount: Float!
  createdAt: DateTime!
  description: String
  id: ID!
  type: String!
}

type TransactionsPage {
  data: [TransactionTypeGQL!]!
  total: Int!
}